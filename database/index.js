const { insertReport } = require('./src/database/reportManager');

console.log("B-kisisi: abuse_reports + full schema (users/files/reports) hazır. reportManager üzerinden rapor ekleyebilirsiniz. Demo eklenmedi.");

